Error message of some sort when redirected due to invalid permissions
Better handling of multiple folders with htaccess
Handle errors better when there is no scanner
Die after redirecting
Unique constraint on Facebook ID in users table
Index on Facebook ID in users table
Create connection to database
    Move code for querying into useable place
    Add new users to database
Security
    When user logs in, query to see if user is found in database
        If not, add user
            Get user name
    Mandate user is logged in
    Give options based on permissions
Store both email and phone number
    Combine facebook IDs that are matching
Automatically run the SQL initialize if not initialized
Interface
Ability to print?
Add ability to login using Facebook
